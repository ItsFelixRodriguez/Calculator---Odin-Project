Calculator
// math functions to write out
//add
function add (a,b) {
    return (a+b);
}
//subtract
function subtract (a,b) {
    return (a - b);
}

//multiply
function multiply (a,b) {
    return (a*b);
}
//divide
function divide (a,b) {
    return (a/b);
}

function operate(){
    //some code here that takes an operator and 2 numbers
    //and then calls one of the functions on the numbers
}

//function to capture number 'a' from 
//inputs on screen buttons

//function to capture number 'b' from 
//inputs on screen buttons
